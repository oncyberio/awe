import {
  getGameData,
  getGameRevision,
  updateGame,
  switchChunk,
  listChunks,
  getChunkConfig,
  setChunkConfig,
  getRoles,
  setRoles,
} from "../actions/game";

export const ClientGameService = {
  getGameData,
  getGameRevision,
  updateGame,
  switchChunk,
  listChunks,
  getChunkConfig,
  setChunkConfig,
  getRoles,
  setRoles,
};
